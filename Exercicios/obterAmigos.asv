function amigos_de_ID = obterAmigos(ID, friends, interesses)
    amigos_de_ID = {};
    for i=1:length(friends)
        if(friends{i,1} == ID)
            amigos_de_ID{i} = interesses{friends{i,2}};
            if(friends{i+1,1}~=ID)
                break;
            end
        end
    end
end

